import React from 'react'

const balance = (props) => {
    return (
        <>
            <h4 style={{
            }}>Your Balance</h4>
            <p>${props.amount}.00</p>
        </>
    )
}

export default balance
